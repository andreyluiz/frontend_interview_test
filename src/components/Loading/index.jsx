// @flow
import React from 'react';
import './styles.scss';

/**
 * Simple component to show loading message when some API is being called.
 */
export default () => (
  <div styleName="loading">
    <span>Loading...</span>
  </div>
);
